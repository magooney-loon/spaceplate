// Barrel for the postprocessing extension — import from '$extensions/postprocessing'.
// The Studio-aware hook (usePostProcessing.ts) and PostProcessingExtension.svelte
// stay path imports.

export * from './postprocessing.svelte';
export { BUNDLED_PP_PRESETS } from './bundledPresets';
export type * from './types';
