// Barrel for the sound extension — import from '$extensions/sound'.
// The Studio-aware hook (useSound.ts) and SoundExtension.svelte stay path imports.

export * from './soundState.svelte';
export type * from './types';
