import type { SkyboxUserPreset } from './types';

/**
 * Bundled skybox presets — committed to git, available in all environments.
 * To add a preset: create it in Studio, click "Copy as Code", paste here, commit.
 */
export const BUNDLED_SKYBOX_PRESETS: SkyboxUserPreset[] = [
	// Example (remove or replace with real presets):
	// {
	//   id: 'bundled-night',
	//   name: 'Night Sky',
	//   createdAt: 0,
	//   snapshot: { id: 'bundled-night', name: 'Night Sky', turbidity: 0.1, ... }
	// }
];
